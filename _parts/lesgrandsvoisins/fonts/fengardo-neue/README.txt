Fengardo Neue — Loïc Sander, SIL OFL (see the name table of each file).
Copied unmodified from lesgrandsvoisins.com (static/core/fonts/); the .ttf files
are the same fonts re-wrapped from WOFF2 to plain TrueType for LuaLaTeX
(no glyph or metadata changes).

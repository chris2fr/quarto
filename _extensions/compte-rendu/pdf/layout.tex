\documentclass[a4paper,11pt,french]{quarto-lettre}
\usepackage{titlesec}

$if(page-footer)$\QLsetfooter{$page-footer$}$endif$
$if(page-header)$\QLsetfirstpageheader{$page-header$}$endif$

$for(header-includes)$
$header-includes$
$endfor$

% ── Margins (YAML: margin-inner, margin-outer, margin-top, margin-bottom) ─────
\geometry{inner=$if(margin-inner)$$margin-inner$$else$20mm$endif$,outer=$if(margin-outer)$$margin-outer$$else$20mm$endif$,top=5mm,bottom=25mm,headheight=15mm,headsep=5mm,includehead}

% ── Section styling ───────────────────────────────────────────────────────────
\titleformat{\section}{\large\bfseries\sffamily}{}{0em}{}[\vspace{-0.5em}\rule{\linewidth}{0.4pt}]
\titleformat{\subsection}{\normalsize\bfseries\sffamily}{}{0em}{}
\titlespacing*{\section}{0pt}{1.2em}{0.5em}
\titlespacing*{\subsection}{0pt}{0.8em}{0.3em}

% ── CR section environments ───────────────────────────────────────────────────

\newenvironment{div-participants}{%
  \section*{Participants}
}{\vspace{0.5em}}

\newenvironment{div-agenda}{%
  \section*{Ordre du jour}
}{\vspace{0.5em}}

\newenvironment{div-body}{%
}{\vspace{0.5em}}

\newenvironment{div-decisions}{%
  \section*{Décisions}
  \begin{leftbar}
}{\end{leftbar}\vspace{0.5em}}

\newenvironment{div-actions}{%
  \section*{Actions}
}{\vspace{0.5em}}

\newenvironment{div-next-meeting}{%
  \section*{Prochaine réunion}
}{\vspace{0.5em}}

\newenvironment{div-approval}{%
  \section*{Approbation du compte-rendu}
  \begin{flushright}
    \begin{minipage}{10cm}
}{%
    \end{minipage}
  \end{flushright}
}

\begin{document}

\thispagestyle{firstpage}

% Title block
\begin{center}
$if(organization)$
  {\small\sffamily\textcolor{gray}{\MakeUppercase{$organization$}}}\\[0.5em]
$endif$
  {\LARGE\bfseries\sffamily Compte-rendu de réunion}\\[0.5em]
  {\large $title$}\\[0.4em]
  {\itshape $date$$if(place)$\enspace---\enspace{}$place$$endif$}\\[0.2em]
$if(ref)$
  {\small Réf.\,: $ref$}\\
$endif$
\end{center}

\vspace{1em}

\thispagestyle{firstpage}
\afterpage{\newgeometry{inner=$if(margin-inner)$$margin-inner$$else$20mm$endif$,outer=$if(margin-outer)$$margin-outer$$else$20mm$endif$,top=$if(margin-top)$$margin-top$$else$25mm$endif$,bottom=$if(margin-bottom)$$margin-bottom$$else$15mm$endif$}}

$body$

\end{document}
